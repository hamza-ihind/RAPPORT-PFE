
\chapter{Mesurer}
\label{cp:mesurer}

Après avoir défini les objectifs et le périmètre du projet dans la phase \textit{Définir}, nous entamons désormais la phase \textit{Mesurer} de la démarche DMAIC. Cette étape consiste à quantifier l'état actuel du processus de vérification qualité, à identifier les critères de conformité critiques et à établir des indicateurs de performance mesurables.

L'objectif de cette phase est double : d'une part, collecter les données nécessaires pour évaluer l'ampleur du problème de contrôle qualité sur les surfaces CATIA, et d'autre part, formaliser les règles métier qui serviront de référence pour automatiser les vérifications. Nous nous concentrons en particulier sur les défauts surfaciques tels que les auto-intersections, qui constituent l'un des principaux obstacles à la fabricabilité des pièces conçues.

Cette partie présente les critères de qualité identifiés, leurs fondements mathématiques, ainsi que les mécanismes de détection que les macros Q-Checker métier devront automatiser.

\section{Convention de nommage}

Une convention de nommage est un ensemble de règles permettant d'attribuer des identifiants uniques et cohérents aux entités d'un système. Dans un environnement CAO tel que CATIA, l'absence d'une nomenclature rigoureuse entraîne des difficultés de recherche dans les gestionnaires de données (ENOVIA, Windchill), des erreurs d'assemblage, des complications lors de l'exécution de macros automatisées, ainsi qu'une perte de temps lors des revues de conception.

La convention retenue pour ce projet suit le format \texttt{XXX-0000}, où \texttt{XXX} représente exactement trois lettres majuscules (code famille ou département), suivi d'un tiret séparateur, puis de trois ou quatre chiffres de séquencement. Par exemple, \texttt{MEC-001} et \texttt{ASS-1234} sont des noms conformes, tandis que \texttt{mec-001} (minuscules), \texttt{MEC001} (absence de tiret) ou \texttt{MEC-001A} (caractère supplémentaire) sont rejetés.

\section{Algorithme de vérification}

L'algorithme implémenté dans la macro VBA contrôle automatiquement la conformité des noms de pièces et d'assemblages CATIA. Il repose sur la détection du type de document actif, l'utilisation d'une expression régulière pour valider le format, un parcours récursif pour inspecter tous les niveaux d'un assemblage, et la génération d'un rapport de non-conformités.

\subsection{Pseudocode de l'algorithme}

% TODO : Insérer ici la capture d'écran du pseudocode / code VBA
\begin{center}
    \textit{[Image du pseudocode à insérer]}
    % \includegraphics[width=\textwidth]{Figures/pseudocode-nommage.png}
\end{center}

\section{Étapes de la macro}

Les principales étapes de fonctionnement de la macro sont les suivantes :

\begin{itemize}
    \item[$\bullet$] \textbf{Récupération du document actif :} la macro accède au document ouvert dans CATIA via \texttt{CATIA.ActiveDocument}.
    \item[$\bullet$] \textbf{Identification du type de document :} distinction entre \texttt{PartDocument} (pièce) et \texttt{ProductDocument} (assemblage) à l'aide de \texttt{TypeName()}.
    \item[$\bullet$] \textbf{Validation du nom (pièce) :} le nom de la pièce est extrait et comparé au pattern \texttt{\textasciicircum[A-Z]\{3\}-\textbackslash d\{3,4\}\$} via une expression régulière.
    \item[$\bullet$] \textbf{Parcours récursif (assemblage) :} pour un assemblage, la macro inspecte récursivement chaque sous-produit et valide son \texttt{PartNumber}.
    \item[$\bullet$] \textbf{Génération du rapport :} les noms non conformes sont collectés et affichés dans un message récapitulatif à l'utilisateur.
\end{itemize}

% TODO : Insérer ici le schéma visuel illustrant le flux de la macro
\begin{center}
    \textit{[Schéma visuel du flux de la macro à insérer]}
    % \includegraphics[width=\textwidth]{Figures/schema-macro-nommage.png}
\end{center}

\section{Critères de qualité surfacique et macros de détection}

Cette section présente les défauts surfaciques critiques identifiés lors de la phase de mesure, ainsi que les macros Q-Checker développées pour automatiser leur détection. Pour chaque défaut, nous exposons sa définition, ses fondements mathématiques, sa justification métier, puis l'algorithme de vérification associé.

%% ============================================================
%%  AUTO-INTERSECTION
%% ============================================================
\subsection{Auto-intersection (Offset Clipping)}

\subsubsection{Définition et fondements mathématiques}

L'auto-intersection (ou \textit{Offset Clipping}) survient lors d'une opération de création d'épaisseur (\textit{Thick Surface}) ou de décalage (\textit{Offset}) sur une géométrie complexe. La nouvelle surface générée se croise alors elle-même, créant une boucle de matière incohérente.

Mathématiquement, la surface décalée $S_d$ est définie par :
\begin{equation*}
    S_d(u,v) = S(u,v) + d \cdot \mathbf{n}(u,v)
\end{equation*}
où $d$ est l'épaisseur et $\mathbf{n}$ la normale unitaire. La condition de validité impose que le rayon de courbure minimal ($R_{min}$) soit strictement supérieur à l'épaisseur appliquée :
\begin{equation*}
    R_{min} > d
\end{equation*}
Lorsque $d \ge R_{min}$, le centre de courbure est dépassé, la normale s'inverse et la topologie devient \textit{Non-Manifold}.

\begin{figure}[hbt!]
    \centering
    \includegraphics[width=0.45\textwidth]{Figures/self-intersection/self-intersection.png}
    \hfill
    \includegraphics[width=0.45\textwidth]{Figures/self-intersection/self-intersection-2.jpg}
    \caption{Illustration du phénomène d'auto-intersection sur une surface complexe.}
    \label{fig:self_intersection}
\end{figure}

\subsubsection{Justification métier : pourquoi détecter ce défaut ?}

Même si CATIA parvient parfois à générer la face, celle-ci reste techniquement inexploitable en aval :

\begin{itemize}
    \item[$\bullet$] \textbf{Fabrication (FAO) :} les trajectoires d'outil CNC échouent ou provoquent des collisions face à une matière croisée.
    \item[$\bullet$] \textbf{Simulation (FEA) :} le maillage volumique devient impossible, rendant les résultats de résistance ou de crash-test invalides.
    \item[$\bullet$] \textbf{Impression 3D :} le \textit{Slicing} ne parvient plus à distinguer l'intérieur de l'extérieur du volume.
\end{itemize}

La détection automatisée de ce défaut est donc indispensable pour garantir la fabricabilité des pièces dès la phase de conception.

\subsubsection{Pseudocode de l'algorithme}

% TODO : Insérer ici la capture d'écran du pseudocode / code VBA
\begin{center}
    \textit{[Image du pseudocode de la macro auto-intersection à insérer]}
    % \includegraphics[width=\textwidth]{Figures/pseudocode-auto-intersection.png}
\end{center}

\subsubsection{Étapes de la macro}

\begin{itemize}
    \item[$\bullet$] \textbf{Sélection de la cible :} la macro identifie l'objet \texttt{HybridShape} (surface) dans le document actif.
    \item[$\bullet$] \textbf{Mesure du rayon de courbure :} appel de la méthode \texttt{GetMinimumCurvatureRadius()} sur la surface.
    \item[$\bullet$] \textbf{Comparaison au seuil :} le rayon mesuré est comparé à l'épaisseur standard du projet ($d$).
    \item[$\bullet$] \textbf{Génération de l'alerte :} si $R_{min} \le d$, la pièce est signalée comme \textit{Non-Manufacturable}.
\end{itemize}

% TODO : Insérer ici l'illustration visuelle des étapes de la macro
\begin{center}
    \textit{[Illustration visuelle des étapes à insérer]}
    % \includegraphics[width=\textwidth]{Figures/etapes-macro-auto-intersection.png}
\end{center}

\subsubsection{Organigramme de la macro}

% TODO : Insérer ici le flowchart de la macro auto-intersection
\begin{center}
    \textit{[Organigramme (flowchart) de la macro à insérer]}
    % \includegraphics[width=0.8\textwidth]{Figures/flowchart-auto-intersection.png}
\end{center}

%% ============================================================
%%  VRILLAGE
%% ============================================================
\subsection{Vrillage (Auto-repliement)}

\subsubsection{Définition et fondements mathématiques}

Le vrillage (ou \textit{auto-repliement}) est une anomalie topologique majeure survenant lors de la génération de surfaces complexes via les fonctions de balayage (\textit{Sweep}) ou de surfaces multi-sections. Il se traduit par une inversion locale de la matière où la surface se croise elle-même.

Pour qu'une surface $\mathbf{S}(u,v)$ soit valide, sa paramétrisation doit être régulière. Le déterminant du Jacobien ($J$) traduit cette régularité :
\begin{equation*}
    J(u,v) = \left\| \frac{\partial \mathbf{S}}{\partial u} \times \frac{\partial \mathbf{S}}{\partial v} \right\|
\end{equation*}

\begin{itemize}
    \item[$\bullet$] \textbf{Surface saine :} $J(u,v) > 0$ sur l'ensemble du domaine paramétrique.
    \item[$\bullet$] \textbf{Vrillage :} apparaît lorsque $J(u,v) \le 0$. La surface perd alors sa propriété de variété (\textit{manifold}) ; les vecteurs tangents deviennent colinéaires, provoquant un effondrement géométrique.
\end{itemize}

Trois causes racines génèrent ce défaut en CAO :
\begin{itemize}
    \item[$\bullet$] \textbf{Défaut de couplage :} mauvaise correspondance des points de fermeture (\textit{Closing Points}) entre sections.
    \item[$\bullet$] \textbf{Conflit de tangence :} vecteurs de tangence opposés aux extrémités forçant une boucle.
    \item[$\bullet$] \textbf{Saturation de l'épine (\textit{Spine}) :} rayon de courbure de la courbe guide inférieur à la demi-largeur de la section balayée.
\end{itemize}

\subsubsection{Justification métier : pourquoi détecter ce défaut ?}

Le vrillage constitue un critère d'exclusion lors du \textit{Q-Checking} :

\begin{itemize}
    \item[$\bullet$] \textbf{Maillage (FEA) :} les solveurs ne peuvent pas traiter des faces auto-pénétrantes, entraînant un échec de simulation.
    \item[$\bullet$] \textbf{Usinage (FAO) :} les normales erronées provoquent des collisions d'outil ou des trajectoires impossibles.
    \item[$\bullet$] \textbf{Qualité perçue (Class A) :} rupture brutale des lignes de reflet (zébras), inacceptable en carrosserie.
\end{itemize}

La macro de détection permet d'identifier ces zones critiques avant toute transmission aux métiers aval.

\subsubsection{Pseudocode de l'algorithme}

% TODO : Insérer ici la capture d'écran du pseudocode / code VBA
\begin{center}
    \textit{[Image du pseudocode de la macro vrillage à insérer]}
    % \includegraphics[width=\textwidth]{Figures/pseudocode-vrillage.png}
\end{center}

\subsubsection{Étapes de la macro}

\begin{itemize}
    \item[$\bullet$] \textbf{Discrétisation :} balayage de la surface sur une grille de points $P_{i,j}$.
    \item[$\bullet$] \textbf{Calcul des normales :} extraction du vecteur unitaire $\mathbf{n}_{i,j}$ en chaque point.
    \item[$\bullet$] \textbf{Test d'inversion :} si $\mathbf{n}_{i,j} \cdot \mathbf{n}_{i+1,j} < 0$, une inversion de normale est détectée.
    \item[$\bullet$] \textbf{Verdict :} la zone est identifiée comme \textit{Twisted Surface} et la validation du modèle est bloquée.
\end{itemize}

% TODO : Insérer ici l'illustration visuelle des étapes de la macro
\begin{center}
    \textit{[Illustration visuelle des étapes à insérer]}
    % \includegraphics[width=\textwidth]{Figures/etapes-macro-vrillage.png}
\end{center}

\subsubsection{Organigramme de la macro}

% TODO : Insérer ici le flowchart de la macro vrillage
\begin{center}
    \textit{[Organigramme (flowchart) de la macro à insérer]}
    % \includegraphics[width=0.8\textwidth]{Figures/flowchart-vrillage.png}
\end{center}

\section{Solutions proposées : Partie surfacique}

Cette partie se concentre sur les critères de qualité surfacique que nous devons contrôler automatiquement via des macros (outils Q-Checker métier). Dans un premier temps, nous abordons la détection de l'auto-intersection.

\subsection{Auto-intersection}

\subsubsection{Définition du Phénomène}

L'\textbf{auto-intersection} survient lorsqu'une surface se croise elle-même dans l'espace 3D, c'est-à-dire lorsque deux zones distinctes du domaine paramétrique de la surface correspondent au même point géométrique. Ce phénomène constitue un défaut topologique majeur, couramment rencontré lors des opérations de création d'épaisseur (\textit{Thick Surface}) ou de décalage (\textit{Offset}) sur des géométries complexes.

\subsubsection{Représentation Paramétrique d'une Surface}

Dans CATIA V5, les surfaces sont modélisées en tant que surfaces NURBS (\textit{Non-Uniform Rational B-Splines}). Une surface est une application paramétrique qui associe à chaque couple $(u, v)$ un point 3D :

\begin{equation*}
    S : D \subset \mathbb{R}^2 \longrightarrow \mathbb{R}^3 \qquad (u, v) \longmapsto S(u, v) = \big( x(u,v),\; y(u,v),\; z(u,v) \big)
\end{equation*}

Le domaine paramétrique $D$ est un rectangle dans le plan UV :

\begin{equation*}
    D = [u_{\min},\; u_{\max}] \times [v_{\min},\; v_{\max}]
\end{equation*}

Les principaux paramètres qui définissent une surface NURBS sont présentés dans le \autoref{tab:params-nurbs}.

\begin{table}[H]
    \centering
    \caption{Paramètres d'une surface NURBS}
    \label{tab:params-nurbs}
    \begin{tabularx}{\textwidth}{|>{\centering\arraybackslash}p{2.5cm}|X|}
        \hline
        \rowcolor{headerblue}
        \textcolor{white}{\textbf{Symbole}} & \centering\textcolor{white}{\textbf{Signification}} \tabularnewline
        \hline
        \rowcolor{rowlight}
        $P_{ij}$ & Points de contrôle (grille 3D qui « sculpte » la forme) \\
        \hline
        \rowcolor{rowwhite}
        $w_{ij}$ & Poids associés à chaque point de contrôle \\
        \hline
        \rowcolor{rowlight}
        $N_{i,p}(u)$ & Fonctions de base B-spline de degré $p$ en direction $u$ \\
        \hline
        \rowcolor{rowwhite}
        $N_{j,q}(v)$ & Fonctions de base B-spline de degré $q$ en direction $v$ \\
        \hline
    \end{tabularx}
\end{table}

\subsubsection{Condition Formelle d'Auto-intersection}

Une surface $S$ présente une auto-intersection s'il existe deux couples de paramètres distincts qui correspondent au même point 3D :

\begin{equation*}
    \exists\; (u_1, v_1) \neq (u_2, v_2) \in D \quad \text{tels que} \quad S(u_1, v_1) = S(u_2, v_2)
\end{equation*}

En d'autres termes, deux zones différentes de la surface occupent la même position dans l'espace. La surface « se traverse elle-même » en formant une courbe d'auto-intersection.

\subsubsection{Fondements Mathématiques du Décalage}

Le succès d'un épaississement dépend de la relation entre la géométrie locale et la distance de décalage :

\begin{itemize}
    \item[•] \textbf{Vecteur Normal :} La surface décalée $S_d$ est définie par :
    \begin{equation*}
        S_d(u,v) = S(u,v) + d \cdot \mathbf{n}(u,v)
    \end{equation*}
    \textit{Où $d$ est l'épaisseur et $\mathbf{n}$ est la normale unitaire.}

    \item[•] \textbf{La Condition de Validité :} Pour éviter toute singularité ou auto-intersection, le rayon de courbure minimal ($R_{\min}$) de la surface support doit être strictement supérieur à l'épaisseur ($d$) appliquée :
    \begin{equation*}
        R_{\min} > d
    \end{equation*}

    \item[•] \textbf{Le Point de Rupture :} Si $d \ge R_{\min}$, le centre de courbure est « dépassé », la normale s'inverse et la topologie devient \textbf{Non-Manifold}.
\end{itemize}

\subsubsection{Impacts Industriels}

L'auto-intersection constitue un défaut critique dans la chaîne numérique. Même si CATIA parvient parfois à générer la face, celle-ci est techniquement inexploitable. Le \autoref{tab:impacts-auto-intersection} résume les conséquences par domaine.

\begin{table}[H]
    \centering
    \caption{Conséquences industrielles de l'auto-intersection}
    \label{tab:impacts-auto-intersection}
    \begin{tabularx}{\textwidth}{|>{\bfseries\raggedright\arraybackslash}p{3.8cm}|X|}
        \hline
        \rowcolor{headerblue}
        \textcolor{white}{\textbf{Domaine}} & \centering\textcolor{white}{\textbf{Conséquence}} \tabularnewline
        \hline
        \rowcolor{rowlight}
        Épaississement / Offset & \textit{ThickSurface} échoue : CATIA ne peut pas calculer un solide cohérent \\
        \hline
        \rowcolor{rowwhite}
        Maillage EF (FEA) & Éléments finis dégénérés $\rightarrow$ résultats de simulation incorrects \\
        \hline
        \rowcolor{rowlight}
        Usinage CNC & Trajectoires d'outils croisées $\rightarrow$ collisions ou enlèvements non désirés \\
        \hline
        \rowcolor{rowwhite}
        Injection plastique & Contre-dépouilles impossibles à démouler dans le moule \\
        \hline
        \rowcolor{rowlight}
        Échange de données & Risque de corruption géométrique lors d'import/export STEP ou IGES \\
        \hline
    \end{tabularx}
\end{table}

\begin{block}[note]
Dans l'industrie automobile, un défaut d'auto-intersection détecté tardivement (après livraison des outillages) peut engendrer des coûts de reprise de plusieurs dizaines de milliers d'euros. D'où l'importance d'une détection automatisée en amont.
\end{block}

\subsubsection{Méthode de Détection : la Technique « Try-to-Join »}

CATIA V5 ne propose aucune méthode API directe du type \texttt{Surface.IsSelfIntersecting()}. L'outil interactif « Face Checker » existe, mais il n'est pas exposé dans l'API d'automatisation VBA/CATScript. La solution adoptée est la technique \textbf{Try-to-Join}, qui consiste à :

\begin{itemize}
    \item[•] Créer un \textit{Join} (\texttt{HybridShapeAssemble}) sur la surface cible.
    \item[•] Forcer le recalcul via \texttt{Part.UpdateObject}.
    \item[•] Capturer l'erreur retournée par le noyau CGM en cas d'auto-intersection.
\end{itemize}

\paragraph{Étapes internes du noyau CGM.}
Lorsque \texttt{Part.UpdateObject} est appelé, le noyau géométrique CGM de Dassault Systèmes effectue les opérations détaillées dans le \autoref{tab:etapes-cgm}.

\begin{table}[H]
    \centering
    \caption{Pipeline de vérification du noyau CGM}
    \label{tab:etapes-cgm}
    \begin{tabularx}{\textwidth}{|>{\centering\arraybackslash}p{1cm}|>{\bfseries\raggedright\arraybackslash}p{3.5cm}|X|}
        \hline
        \rowcolor{headerblue}
        \textcolor{white}{\textbf{N°}} & \textcolor{white}{\textbf{Opération}} & \centering\textcolor{white}{\textbf{Description}} \tabularnewline
        \hline
        \rowcolor{rowlight}
        1 & Tessellation & La surface NURBS est discrétisée en un maillage de triangles par évaluation de $S(u,v)$ sur une grille régulière. \\
        \hline
        \rowcolor{rowwhite}
        2 & Construction BVH & Chaque triangle est enfermé dans une boîte englobante (AABB). Un arbre binaire hiérarchique est construit pour réduire la complexité de $O(n^2)$ à $O(n \log n)$. \\
        \hline
        \rowcolor{rowlight}
        3 & Test Triangle-Triangle & Pour chaque paire de triangles dont les boîtes se chevauchent, un test d'intersection géométrique exact est réalisé (calcul de plans et intervalles de projection). \\
        \hline
        \rowcolor{rowwhite}
        4 & Filtrage des adjacences & Les triangles voisins partageant une arête sont exclus car leur contact est normal. \\
        \hline
        \rowcolor{rowlight}
        5 & Raffinement NURBS & Si des candidats sont trouvés, le noyau résout le système $S(u_1,v_1) - S(u_2,v_2) = \mathbf{0}$ par la méthode de Newton-Raphson pour confirmer l'intersection. \\
        \hline
        \rowcolor{rowwhite}
        6 & Signalement d'erreur & Si une solution $(u_1^*,v_1^*) \neq (u_2^*,v_2^*)$ est trouvée, une erreur topologique est levée et capturée par le bloc \texttt{On Error}. \\
        \hline
    \end{tabularx}
\end{table}

\subsubsection{Stratégies de Correction}

Dès qu'une auto-intersection est repérée, plusieurs stratégies peuvent être envisagées pour la résoudre :

\begin{itemize}
    \item[•] \textbf{Augmentation du Rayon :} Modifier les congés (\textit{Fillets}) pour que $R > d$.
    \item[•] \textbf{Healing Assistant :} Utiliser les outils de lissage pour éliminer les micro-variations de courbure.
    \item[•] \textbf{Découpe locale :} Supprimer la zone de conflit et reconstruire une transition propre via un \textit{Multi-sections Surface} ou un \textit{Blend}.
\end{itemize}

\subsubsection{Limites et Perspectives}

Le \autoref{tab:limites-auto-intersection} résume les principales limites identifiées dans la détection automatisée de l'auto-intersection.

\begin{table}[H]
    \centering
    \caption{Limites de la détection automatisée de l'auto-intersection}
    \label{tab:limites-auto-intersection}
    \begin{tabularx}{\textwidth}{|>{\bfseries\raggedright\arraybackslash}p{3.8cm}|X|}
        \hline
        \rowcolor{headerblue}
        \textcolor{white}{\textbf{Limite}} & \centering\textcolor{white}{\textbf{Explication}} \tabularnewline
        \hline
        \rowcolor{rowlight}
        Faux négatifs possibles & Si l'auto-intersection est inférieure à la tolérance interne CGM ($\approx 10^{-3}$~mm), le Join peut réussir sans la détecter. \\
        \hline
        \rowcolor{rowwhite}
        Faux positifs possibles & Une surface avec des discontinuités de tangence marquées peut faire échouer le Join sans être réellement auto-intersectante. \\
        \hline
        \rowcolor{rowlight}
        Performance & Pour les assemblages volumineux ($> 500$ surfaces), la création/suppression de Joins temporaires peut être lente. \\
        \hline
        \rowcolor{rowwhite}
        Type de document & Le macro ne traite pas les CATDrawing ni les CATAnalysis. \\
        \hline
    \end{tabularx}
\end{table}

Parmi les améliorations envisageables :

\begin{itemize}
    \item[•] \textbf{Coloration visuelle :} Colorier en rouge les surfaces défaillantes via \texttt{VisProperties.SetRealColor}.
    \item[•] \textbf{Export CSV :} Écrire le rapport dans un fichier pour traçabilité et intégration dans les processus qualité.
    \item[•] \textbf{Seuil de tolérance configurable :} Permettre à l'utilisateur de définir la tolérance angulaire et positionnelle.
    \item[•] \textbf{Chaîne de contrôle complète :} Chaîner avec la vérification de vrillage, sliver faces et solidité.
\end{itemize}
